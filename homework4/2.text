n≥5 時沒有「封閉形式公式」，所以要用數值法求根。我的想法是把多項式視為複數平面上的函數 
𝑃
(
𝑧
)
P(z)，用迭代法逼近所有根。為了能一次得到全部根，選擇 Durand–Kerner（Weierstrass）法：同時維護 n 個根的估計值 
{
𝑧
𝑖
}
{z
i
	​

}，反覆用

𝑧
𝑖
←
𝑧
𝑖
−
𝑃
(
𝑧
𝑖
)
∏
𝑗
≠
𝑖
(
𝑧
𝑖
−
𝑧
𝑗
)
z
i
	​

←z
i
	​

−
∏
j

=i
	​

(z
i
	​

−z
j
	​

)
P(z
i
	​

)
	​


更新，直到每個 
𝑧
𝑖
z
i
	​

 的變動量很小（或 
∣
𝑃
(
𝑧
𝑖
)
∣
∣P(z
i
	​

)∣ 很小）。這方法不需要導數（不像 Newton），而且可自然處理複數根，符合題目「沒有實根也要回傳複數根」。初始值可放在一個圓上（用簡單的根界估計半徑），再加微小擾動避免對稱造成分母接近 0。最後用代回驗證 
𝑃
(
𝑧
𝑖
)
≈
0
P(z
i
	​

)≈0（例如 abs(P(z_i)) < 1e-8 或 cmath.isclose）。